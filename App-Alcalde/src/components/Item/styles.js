import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    itemContainer: {
        margin: 10,
        padding: 10,
        borderRadius: 5,
        backgroundColor: "red",
      },
      item: {
        padding: 10,
        textAlign: "center",
      },
  });